import 'package:flutter/material.dart';

class LeaveManagementLayout extends StatelessWidget {
  const LeaveManagementLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('leave management'));
  }
}

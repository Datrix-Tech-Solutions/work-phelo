import 'package:flutter/material.dart';

const myContentPadding = EdgeInsets.all(10);

const menuItemPadding = EdgeInsets.only(
  top: 10,
  left: 20,
  right: 20,
  bottom: 10,
);

const formWidgetPadding = EdgeInsets.only(
  top: 16,
  bottom: 10,
  left: 10,
  right: 10,
);

const formPadding = EdgeInsets.only(left: 20, right: 20);

const space = EdgeInsets.only(top: 20);
